def fact(n):
    if n==0 or n==1:
        return 1
    else:
        factorial=1
        for i in range(1,n+1):
            factorial*=i
        return factorial


n=int(input("enter the number:"))
print(f"factorial of {n} is : {fact(n)}")


